# Vocofo TODO

## High Priority
- [ ] Search/filter — `/` opens search bar, filters items as you type
- [ ] Overwrite confirmation popup — paste on existing file shows "Overwrite? [Y/N]" dialog

## Medium Priority
- [ ] Sync panel path — `=` sets inactive panel to same directory as active
- [ ] File details in listing — show permissions, size, date (like `ls -la`)
- [ ] Bump version to 1.0.0

## Low Priority (nice-to-have)
- [ ] Bookmarks — save frequently visited paths, quick access via number keys
- [ ] Bulk selection — Space to mark multiple files, operate on selection
- [ ] Config file — `~/.config/vocofo/config.toml` for colors, keybindings, default directory
